version: "3.9"

services:
  web:
    # Build a production image (multi-stage) and serve via Nginx
    build:
      context: .
      dockerfile: Dockerfile
    image: tubetrend:local
    ports:
      - "3000:80" # Open http://localhost:3000
